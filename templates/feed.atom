<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">

<title>King'ori.co</title> 
<subtitle>King'ori.co Articles</subtitle>
<link href="http://{{ page.base_url }}/{{ page.permalink }}" rel="self" type="application/atom+xml"/>
<link href="http://{{ page.base_url }}/" rel="alternate" type="text/html"/>
<icon>http://{{ page.base_url }}/favicon.ico</icon>
<logo>http://{{ page.base_url }}/images/logo-dark-24@2x.png</logo>
<updated>{{ page.updated }}</updated>
<author> 
    <name>{{ page.author }}</name>
    <email>{{ page.email }}</email>
    <uri>http://{{ page.base_url }}/</uri>
</author>
<id>{{ page.domain_name }}:{{ page.permalink }}</id>
<rights>© {{ page.current_year }} {{ page.site_name }}</rights>

{# 'page' is '/' #}
{% block context %}
{% set page = get('/articles') %}

    {# 'page' is '/articles' #}

    {% if page.children %}
        {% for year in page.children | reverse %}

            {% if year.children %}
                {% for month in year.children | reverse %}

                    {% if month.children %}
                        {% for article in month.children | reverse %}

                            <entry>
                                <title>{{ article.title }}{% if  article.type == 'linked-post' %} ↩{% endif %}</title>
                                <link rel="alternate" type="text/html" href="{{ article.link }}" />
                                <id>{{ article.domain_name }}:{{ article.permalink }}</id>
                                <published>{{ article.published | date("c")}}</published>
                                <updated>{{ article.updated }}</updated>
                                <content type="xhtml" xml:lang="en">
                                    <div xmlns="http://www.w3.org/1999/xhtml">
                                        {{ article.content }}
                                        {% if  article.type == 'linked-post' %}
                                            <p>
                                                <a title="Permalink to '{{ article.title }}'" href="http://{{ page.base_url }}/{{ article.permalink }}" target="_blank">&nbsp;↩&nbsp;</a>
                                            </p>
                                        {% endif %}
                                    </div>
                                </content>
                                <category term="articles"/>
                                <author>
                                    <name>{{ article.author }}</name>
                                    <email>{{ article.email }}</email>
                                    <uri>http://{{ article.base_url }}/</uri>
                                </author>
                            </entry>

                        {% endfor %}
                    {% endif %}

                {% endfor %}
            {% endif %}

        {% endfor %}
    {% endif %}

{% endblock %}
{# the page variable is set back to '/' #}

</feed>